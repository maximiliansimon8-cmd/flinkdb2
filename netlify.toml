[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# All redirects are in public/_redirects (copied to dist/ at build time)
# _redirects takes priority over netlify.toml redirects
